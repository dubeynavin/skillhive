# Example .env for backend
# Copy this to .env and fill real values before running

MONGO_URI=mongodb://localhost:27017
DB_NAME=skillhiveai
FRONTEND_URL=http://localhost:5173
JWT_SECRET=your_jwt_secret_here

# Note: main.go currently listens on :8080 (hard-coded). To change the port,
# update main.go to use os.Getenv("PORT") and fallback to :8080 if empty.
